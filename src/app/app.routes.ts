import { Routes } from '@angular/router';
import { FilesViewComponent } from './file-upload/files-view/files-view.component';

export const routes: Routes = [
  {
    path: '',
    component: FilesViewComponent
  }
];
